const {getPool}=require("./_db");module.exports=async(req,res)=>{if(req.method!=="GET")return res.status(405).json({error:"Method not allowed"});try{const pool=getPool();const [rows]=await pool.query(`SELECT id,group_no,student1_enrollment_no,student2_enrollment_no,project_title,created_at FROM projects ORDER BY created_at DESC,id DESC`);return res.status(200).json({projects:rows})}catch(err){console.error("PROJECT LIST ERROR:",err);return res.status(500).json({error:"Unable to load registered projects.",details:err.message})}};